import React from "react";
import styles from "./assets/scss/Header.scss";

function Header(props) {
  return (
    <h1 className={styles.Header}>
      CSS in JS: Stlyed Component(Less & Styleable)
    </h1>
  );
}

export default Header;
